#include <stdio.h>
/**
  *main - Entry point
  *Description: 'print numbers in base 16 in lowercase'
  *Return: always 0
  */
int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (n = 97; n <= 102; n++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
